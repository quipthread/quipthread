{
  "name": "quipthread",
  "private": true,
  "workspaces": ["apps/*", "packages/*", "embed"],
  "scripts": {
    "dev:dashboard": "bun --cwd apps/dashboard run dev",
    "dev:embed":     "bun --cwd embed run dev",
    "dev:website":   "bun --cwd apps/website run dev",
    "build:dashboard": "bun --cwd apps/dashboard run build",
    "build:embed":     "bun --cwd embed run build",
    "build:website":   "bun --cwd apps/website run build",
    "build": "bun run build:embed && bun run build:dashboard && bun run build:website",
    "build:assets": "bun run build:embed && bun run build:dashboard && mkdir -p backend/static/dashboard && cp embed/dist/embed.iife.js backend/static/embed.js && cp -r apps/dashboard/dist/. backend/static/dashboard/",
    "build:cli": "bun --cwd packages/create-quipthread run build",
    "dev:cli": "bun --cwd packages/create-quipthread run dev",
    "test:backend": "cd backend && go test ./..."
  }
}
